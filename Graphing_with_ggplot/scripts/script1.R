# Replace the blanks with the proper code

# the original data is called hemlocks - assign the filtered data set the name hemlocks.h

# the variable you want to remove the NA's from is height

# remember - the print code at the bottom is there so that the code in this script will be run in the console

# When you are done, save this script and type submit() in the console

_________ <- __________ %>%
                filter(!is.na(_________)) %>%
print